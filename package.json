{
  "name": "mock-courses-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.12.2"
  },
  "scripts": {
    "start": "json-server --watch db.json --delay 1000 --static ./public",
    "startnd": "json-server --watch db.json --static ./public"
  }
}
